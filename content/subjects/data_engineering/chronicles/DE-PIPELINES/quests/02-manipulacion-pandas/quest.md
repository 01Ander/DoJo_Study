# Quest 02: Manipulación con Pandas

**Nivel de Scaffolding: 3 (Escribir GIVEN/WHEN/THEN)**

En este nivel, no hay código de testing escrito. Tienes la estructura de los tests en forma de comentarios GIVEN/WHEN/THEN. Tu trabajo es traducir esos comentarios a código `pytest` real y luego implementar la función.

## Objetivos
1. Escribe el código de los tests en `test_02_pandas.py` guiándote por los comentarios.
2. Implementa `process_silver_inventory()` usando Pandas (`dropna`, `str` accessors, `astype`).

## Comandos
```bash
pytest test_02_pandas.py
```
