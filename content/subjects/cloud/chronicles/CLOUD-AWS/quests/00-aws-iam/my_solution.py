import json
import os

def verificar_permisos(policy_path: str, action_requerida: str) -> bool:
    """
    Lee un archivo JSON de política de IAM y verifica si permite una acción específica.
    """
    # 1. Abre el archivo en modo lectura
    
    # 2. Carga el JSON a un diccionario de Python
    
    # 3. Itera sobre los 'Statement' buscando la acción requerida y 'Effect' == 'Allow'
    
    pass
